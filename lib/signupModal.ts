// How long after the user responds to the geolocation prompt (Allow or
// Don't Allow) before the signup modal appears. Change this one value to
// adjust the delay.
export const SIGNUP_MODAL_DELAY_MS = 500;

// Cookie used to suppress the modal once it's been shown to a visitor.
export const SIGNUP_MODAL_COOKIE = 'lgl_signup_modal_seen';
export const SIGNUP_MODAL_SUPPRESS_DAYS = 7;
